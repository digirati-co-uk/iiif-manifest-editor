import { scss as css } from "@acab/ecsstatic";

export const promptContainer = css`
  background: #e4e7f0;
  padding: 1em 0.5em;
  border-radius: 3px;
  text-align: center;
  font-weight: 500;
  button {
    margin: 1em auto;
  }
`;
