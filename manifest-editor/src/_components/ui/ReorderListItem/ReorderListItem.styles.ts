import styled from "styled-components";

export const HandleContainer = styled.div`
  font-size: 1.2em;
  display: flex;
  padding: 0.1em;
  border-radius: 3px;
  margin: auto 0.2em;
  aspect-ratio: 1;

  &:hover {
    background: #eee;
  }
`;
